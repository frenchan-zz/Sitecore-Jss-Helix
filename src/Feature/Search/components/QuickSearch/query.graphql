query Search($searchCriteria: String) {
    search(keyword:$searchCriteria) {
        results {
            items {
                name,
                path
            },
            totalCount
        }
    }
}